import * as React from "react";

function NotFound() {
    return (
        <div className="NotFound">
            Sorry, We couldnt find What you were searching for.
        </div>
    );
  }
  
  export default NotFound;